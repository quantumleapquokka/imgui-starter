This project adds a simple logging system to the provided Dear ImGui starter project.
The logger is implemented as a singleton so there is only one logging instance that can be used anywhere in the code.
Log messages are shown in a Dear ImGui debug console window and are also written to a log file (app.log) so they can be reviewed after the program runs.

This project was developed and tested on Windows.